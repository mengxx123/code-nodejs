let config = {
    formatedComment: true,
    // 数据库配置
    mysql: {
        host: 'localhost',
        user: 'root',
        database: 'ys_erp',
        password: '123456'
    },
    // 导出的 JSON 文件空格缩进
    indent: 4
}

module.exports = config
